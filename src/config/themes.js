const light = {
  backgroundColor: '#fff',
  fontColor: '#000',
  linkColor: '#000'
};

const dark = {
  backgroundColor: '#000',
  fontColor: '#fff',
  linkColor: '#fff'
};

export default {
  light,
  dark
};
